import React from 'react';
import Navigation from './Navigation';

// FarmerNavigation is just an alias for Navigation
// since farmers use the same navigation component
const FarmerNavigation = () => {
  return <Navigation />;
};

export default FarmerNavigation;
